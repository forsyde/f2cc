/**
 * @namespace f2cc
 * @brief Contains all classes and functions used in the application.
 *
 * In order to avoid potential name clashes, all classes and functions used
 * by the application reside in a namespace. This is essential if the source
 * code is integrated into another application.
 */

/**
 * @namespace f2cc::Forsyde
 * @brief Contains all classes used in the internal representation of a ForSyDe 
 *        model.
 *
 * The \c Forsyde namespace contains all the model, process, ports classes used 
 * to construct the internal representation of a ForSyDe model. It also contains
 * classes which operate on such models.
 */
